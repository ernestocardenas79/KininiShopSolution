
# KininiShop

this follow the course of anartz just as it is, later will be migrated to typegraphql and maybe nestjs with nx

enable docker
cd into docker dependencies branch
run docker compose up -d

cd into  KininiSolution folder
execute npm run dev

browse graphql UI on `https://studio.apollographql.com/sandbox/explorer`
